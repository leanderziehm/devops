services:
  navidrome:
    image: docker.io/deluan/navidrome:latest
    user: 1000:1000 # checked already 
    ports:
      - "4533:4533"
    restart: unless-stopped
    environment:
    volumes:
      - "navidrome-data:/data" 
      - "/var/home/user/Music/##Music:/music:ro,Z" # is the ## in the name a issue?
volumes:
  navidrome-data: